---
description: Project structure and technology stack for project2
globs: project2/**/*
---

# Project Overview

This project is a full-stack application consisting of a Next.js frontend and a FastAPI backend.

## Directory Structure

- **nextjs_fastapi/**: Python backend using FastAPI.
- **nextjs_testing/**: Frontend application using Next.js.

## Technology Stack

### Backend (`nextjs_fastapi`)
- **Framework**: FastAPI
- **Language**: Python
- **Entry Point**: `main.py`
- **Key Features**:
  - Audio processing (upload, waveform, effects)
  - Hughes data synchronization
  - CORS configuration for frontend integration
- **Routes**:
  - `/api/audio`: Audio file management
  - `/api/waveform`: Waveform generation
  - `/api/effects`: Audio effects processing
  - `/api/hughes`: Hughes data integration

### Frontend (`nextjs_testing`)
- **Framework**: Next.js 15.2.4 (App Router)
- **Language**: TypeScript
- **Styling**: Tailwind CSS v4
- **UI Components**: 
  - Radix UI primitives
  - Lucide React icons
  - Framer Motion for animations
  - Recharts for charts
- **Key Libraries**:
  - `three`: 3D graphics
  - `rss-parser`: RSS feed parsing
  - `liquid-glass-react`: UI effects

## Development Conventions

- **Ports**:
  - Frontend: `http://localhost:3000`
  - Backend: `http://localhost:8000`
- **API Communication**:
  - Frontend communicates with backend via HTTP requests to `http://localhost:8000` (or configured production URL).
  - CORS is enabled on the backend to allow requests from the frontend.
